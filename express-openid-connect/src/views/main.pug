extends layout.pug
block content
    h2(class="text-2xl font-medium")= title
    div 
        p(class="text-xl text-gray-700") Welcome to the 
            span(class="font-medium text-gray-900") DEMO 
            | Client 
        p(class="text-3xl text-gray-700") Powered by 
            span(class="font-medium text-gray-900") ExpressJS
    a(href=`${login_path}`)
        button(autofocus, class="flex-inline w-full p-2 text-sm font-normal text-white transition duration-100 rounded-md bg-gradient-to-r from-blue-600 to-blue-400 focus:outline-none focus:ring-2 focus:ring-blue-300 hover:shadow-lg")
            span(class="flex items-center justify-center gap-2 mr-2") 
                img(class="w-5 text-xs" src="/icons/logo-letter.png", alt="unfurl_logo") 
                | Connect with Unfurl